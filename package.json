{
  "name": "mystic-beasts-evolution",
  "version": "1.0.0",
  "description": "Mystic Beasts Evolution - Mobile Idle Merge Game",
  "main": "index.html",
  "scripts": {
    "build": "node build.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:add-ios": "npm install @capacitor/ios@^6.0.0 --strict-ssl=false --legacy-peer-deps && npx cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "author": "Samjo",
  "license": "MIT"
}
